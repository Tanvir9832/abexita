import Navbar from './components/Layout/Navbar';
import './global.css';

export const metadata = {
  title: 'Welcome to price',
  description: 'Generated by create-nx-workspace',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        {children}
      </body>
    </html>
  );
}
